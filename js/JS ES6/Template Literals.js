// Template Literals


// แบบเดิม

const  name = "Petch"
const last="Settapak"
const city = "BKK"

const all= name+last+city
console.log(all);

// แบบใหม่

const allname = `Name = ${name} 
Last = ${last} 
City = ${city}`

console.log(allname);
